Web app was created using following settings:

```
npx nx generate @nx/angular:application
  --directory=apps/web
  --addTailwind=true
  --port=5173
  --prefix=mol
  --skipTests=true
  --zoneless=true
  --no-interactive
```
