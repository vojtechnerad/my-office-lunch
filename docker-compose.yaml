services:
  postgres:
    image: postgres:18-bookworm
    restart: unless-stopped
    container_name: mol-postgres-db
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=user
      - POSTGRES_PASSWORD=development-database-password
      - POSTGRES_DB=mol
